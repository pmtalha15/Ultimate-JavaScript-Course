// try to chage the 4th character of a string. were you able to do it?

let names = "talha";
names[3] = "2";
console.log(names);

// No, Iam not ablew to do it because string is immutable, which means cannot be changed.