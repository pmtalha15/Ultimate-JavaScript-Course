// use type of operator to find the datatype of string in Question1.
let a = "Talha";
console.log(a + 1);

// If we add a number to the string, it will add the number next to the string and the whole value will be treated as a string.

console.log(typeof a); // Prints string